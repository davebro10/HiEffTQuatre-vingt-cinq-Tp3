using System.Collections.Generic;

namespace client
{
    internal class Service
    {
        /*
        public static client CurrentClient { get; set; }

        public fichier CreateFile(client creator, groupe group, string name)
        {
            return null;
        }

        public bool DeleteFile(fichier file)
        {
            return true;
        }

        public bool ModifyFile(fichier file, client client)
        {
            return true;
        }

        public groupe CreateGroup(client creator, string name)
        {
            return null;
        }

        public bool DeleteGroup(groupe group)
        {
            return true;
        }

        public bool InviteMemberToGroup(groupe group, client member)
        {
            return true;
        }

        public bool AddMemberToGroup(groupe group, client member)
        {
            group.client1.Add(member);
            return true;
        }

        public bool RemoveMemberFromGroup(groupe group, client member)
        {
            return true;
        }

        public bool SetGroupAdministrator(groupe group, client admin)
        {
            return true;
        }

        public client Authentificate(string username, string password)
        {
            return null;
        }


        #region TO-DELETE

        public static List<client> LstClients = new List<client>()
        {
            new client()
            {
                id_client = 1,
                nom = "Robert",
                usager = "Robert",
                motdepasse = "Robert"
            },
            new client()
            {
                id_client = 2,
                nom = "Merlin",
                usager = "Merlin",
                motdepasse = "Merlin"
            },
            new client()
            {
                id_client = 3,
                nom = "Jeannette",
                usager = "Jeannette",
                motdepasse = "Jeannette"
            },
        };
        public static List<fichier> Lstfichiers = new List<fichier>()
        {
            new fichier()
            {
                id_fichier = 1,
                id_groupe_fk = 1,
                nom = "FichierDeTest.txt"
            },
            new fichier()
            {
                id_fichier = 2,
                id_groupe_fk = 1,
                nom = "Fiction.txt"
            },
            new fichier()
            {
                id_fichier = 3,
                id_groupe_fk = 2,
                nom = "TroisièmeFichier.txt"
            },
        };

        public static List<groupe> LstGroupes = new List<groupe>()
        {
            new groupe()
            {
                id_groupe = 1,
                admin = 1,
                nom = "Le groupe de Bob"
            },
            new groupe()
            {
                id_groupe = 2,
                admin = 2,
                nom = "Les magiciens d'Ors"
            }
        };

        #endregion

        */
    }
    
}
